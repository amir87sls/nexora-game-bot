# Nexora Game Bot

Upload these files to the root of the GitHub repository.

Render:
- Runtime: Python 3
- Build: pip install -r requirements.txt
- Start: python bot.py
- Environment variable: BOT_TOKEN

After first /start, the first Telegram user becomes the permanent owner.
